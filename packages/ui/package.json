{
    "name": "@hms/ui",
    "version": "0.0.1",
    "private": true,
    "description": "Shared UI components for HMS monorepo",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "lint": "eslint src/ --ext .ts,.tsx",
        "type-check": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "@hms/eslint-config": "workspace:*",
        "@hms/tsconfig": "workspace:*",
        "typescript": "^5.7.3",
        "rimraf": "^6.0.1"
    },
    "peerDependencies": {
        "react": ">=18",
        "react-dom": ">=18"
    }
}